# Rejected Patterns

> Patterns the operator has corrected or rejected. Maxim will avoid these.
> NEVER delete entries — only add.
> Format: [DATE] [CONTEXT] what was rejected → what was preferred

<!-- Example entries:
[2026-04-13] [code] Don't add trailing summaries after completing work → just show the diff
[2026-04-13] [docs] Don't use emoji in documentation files → keep professional
[2026-04-13] [planning] Don't auto-execute without confirmation → always propose then wait
-->
